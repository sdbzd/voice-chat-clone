{
  "name": "voice-chat-clone",
  "version": "1.0.0",
  "description": "Voice dialogue with voice cloning — Cloudflare Worker",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "format": "prettier --write src/"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
